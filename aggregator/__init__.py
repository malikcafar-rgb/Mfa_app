"""Diplomat Daily Briefing — news aggregation pipeline.

Fetches reliable sources (RSS + official government sites), classifies and
clusters stories, optionally summarizes them with Claude, and renders a static
HTML dashboard plus a dated daily brief.
"""

__version__ = "0.1.0"
